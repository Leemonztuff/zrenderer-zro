version: '3.8'

services:
  zrenderer:
    build: .
    ports:
      - "11011:11011"
    volumes:
      - ./zrenderer.docker.conf:/zren/zrenderer.conf
      - ./output:/zren/output
      - ./resources:/zren/resources # Mount your extracted RO assets here
    restart: unless-stopped

  # Placeholder for your RPG Game Backend/Frontend
  # game-app:
  #   image: node:20
  #   working_dir: /app
  #   volumes:
  #     - .:/app
  #   environment:
  #     - ZRENDERER_URL=http://zrenderer:11011
  #     - SUPABASE_URL=${SUPABASE_URL}
  #     - SUPABASE_KEY=${SUPABASE_KEY}
  #   depends_on:
  #     - zrenderer
  #   command: npm start
